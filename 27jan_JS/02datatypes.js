"use strict"; //treat all JS code as never version

//alert(3+3) //we are using nodejs, not browser nece this will not work as it works in browser

console.log(3+3); //code credibility should be high

console.log("Lavanya");

let name = "Lavanya"
let age = 19
let isLoggedIn = false
let state;

console.log(typeof state );